# OSCAR — Operational Supply Chain Autonomous Responder — dependencies.

# Strict typed State Ledger schema + validation (State Mutation Layer enforcement).
pydantic>=2.6,<3.0

# Model Context Protocol servers for the Observation Tools (ERP / Inventory-WMS / Logistics-TMS).
mcp>=1.2.0

# Unified Google Gen AI SDK — the Incident Commander reasoning core.
google-genai>=1.0.0

# Loads GEMINI_API_KEY / GEMINI_MODEL from a local .env file (keeps secrets out of code).
python-dotenv>=1.0.0

# Streamlit cockpit ("Incident Command Center") that watches the State Ledger.
streamlit>=1.30.0

# Cross-platform colored terminal output for the Agent CLI operator console.
colorama>=0.4.6

# Test suite (offline, deterministic smoke tests).
pytest>=8.0
